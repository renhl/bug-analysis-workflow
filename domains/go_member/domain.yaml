# ============================================================
# 业务域元信息 — go_member
# 修改此文件后无需重启，调用 DomainLoader.reload() 即可热生效
# ============================================================

name: go_member
description: "Go 语言会员服务业务域，覆盖会员购买、代扣、敏感城市拦截等核心流程"
owner: member-team
enabled: true

# ------------------------------------------------------------
# 数据库 key
# 引用 config/config.yaml → databases.go_member
# 实际连接参数（密码等）在 config/config.yaml（gitignore，不提交）
# ------------------------------------------------------------
database_key: go_member

# ------------------------------------------------------------
# 知识库 ID（可选，仅当本域使用独立 KB 时才填）
# 不填 = 自动使用 config/config.yaml 中的全局 KB
# ------------------------------------------------------------
# weknora_kb_ids:
#   bug_cases: kb-go-member-001    # 仅本域的 Bug 案例库
#   business_rules: kb-go-member-002
